# Copy this to .env.local for local development.
# For Vercel: set NEXT_PUBLIC_API_URL in the project's Environment Variables dashboard.

# Backend URL — points to your local FastAPI or the Render deployment
NEXT_PUBLIC_API_URL=http://localhost:8000
