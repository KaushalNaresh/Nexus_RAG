# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ── LangChain Ecosystem ───────────────────────────────────────────────────────
langchain>=0.3.7
langchain-openai>=0.2.8
langchain-community>=0.3.7
langchain-core>=0.3.18
langchain-text-splitters>=0.3.3

# ── OpenAI ────────────────────────────────────────────────────────────────────
openai>=1.54.0

# ── Pinecone (Vector DB + Hybrid BM25) ────────────────────────────────────────
pinecone>=5.0.0
pinecone-text>=0.9.0

# ── Embeddings & Cross-Encoder Reranking ──────────────────────────────────────
# Pinned for compatibility with torch 2.x available on linux/amd64
sentence-transformers==3.3.1
transformers==4.44.2
torch>=2.2.0,<2.6.0

# ── Input Guardrails (NeMo — pattern matching + Colang flows) ─────────────────
nemoguardrails>=0.10.0

# ── Evaluation ────────────────────────────────────────────────────────────────
ragas>=0.2.0
datasets>=3.1.0

# ── Document Loading ──────────────────────────────────────────────────────────
pymupdf>=1.24.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── HTTP & Async ──────────────────────────────────────────────────────────────
httpx>=0.27.0
aiofiles>=24.1.0

# ── Logging ───────────────────────────────────────────────────────────────────
structlog>=24.4.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
